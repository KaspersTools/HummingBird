#include "Application.h"

int main() {
//    rfbClient* client = NULL;
//
//    client = rfbGetClient(8, 3, 4); // for 32-bit pixels
//    client->serverHost = strdup("192.168.2.25");
//    client->serverPort = 5900;
//    char* dummy_argv[] = {NULL, NULL};  // 'program_name' can be anything
//    int dummy_argc = 1;  // Only one string in dummy_argv
////
//    if (!rfbInitClient(client, &dummy_argc, dummy_argv)) {
//        fprintf(stderr, "Failed to initialize VNC client\n");
//        return -1;
    //}

    KBTools::Application app;
    return 0;
}
